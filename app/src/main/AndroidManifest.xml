<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hyx.billiardball">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        
        <activity
            android:name=".activity.HelloActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.WindowActivity"
            android:launchMode="singleTask"/>
        <activity
            android:name=".activity.SelectActivity"
            android:launchMode="singleTask"/>
        <activity
            android:name=".activity.RoomActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.findRoomActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.ClientWindowActivity"
            android:launchMode="singleTask"/>
        <activity
            android:name=".activity.ServerWindowActivity"
            android:launchMode="singleTask"/>

    </application>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

</manifest>